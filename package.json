{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "Task management app with a Kanban board dashboard",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
